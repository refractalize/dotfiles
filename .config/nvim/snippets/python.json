{
  "db": {
    "prefix": [
      "db"
    ],
    "body": [
      "print(\"$1\", $1)"
    ]
  },
  "test": {
    "prefix": [
      "test"
    ],
    "body": [
      "def test_$1():",
      "\t$0"
    ]
  },
  "sql": {
    "prefix": [
      "sql"
    ],
    "body": [
      "# FIXME: remove this",
      "with open(\"debug.sql\", \"w\") as f:",
      "\tf.write(str($1.compile(compile_kwargs={\"literal_binds\": True})))"
    ]
  }
}
